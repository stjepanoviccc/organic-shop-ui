import styles from './ReviewForm.module.scss';

const ReviewForm = () => {
    return (
        <div className={styles.reviewForm}>
            Review Form;
        </div>
    )
};

export default ReviewForm;